import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function MapScreen() {
  return (
    <ScreenPlaceholder
      title="Map"
      subtitle="Your stylized exploration map. Walk around to discover plants near you."
      milestone="M2 — Map & geolocation"
    />
  );
}
