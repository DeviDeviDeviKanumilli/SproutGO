import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function PlantDexScreen() {
  return (
    <ScreenPlaceholder
      title="PlantDex"
      subtitle="Your personal collection of discovered species, plus the full Library."
      milestone="M3 — Library & PlantDex"
    />
  );
}
