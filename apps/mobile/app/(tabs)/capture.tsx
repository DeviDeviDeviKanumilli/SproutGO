import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function CaptureScreen() {
  return (
    <ScreenPlaceholder
      title="Capture"
      subtitle="Photograph a plant and let AI identify it. The core discovery loop."
      milestone="M1 — Core discovery loop"
    />
  );
}
